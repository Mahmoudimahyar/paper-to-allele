# Execution plans

Complex tasks use checked-in plans so work survives agent/session boundaries.
- `active/`: current plans.
- `completed/`: finished plans retained for history.
- `templates/`: plan format.

A plan contains objective, non-goals, dependencies, acceptance commands, progress, decisions, verification evidence, and rollback. Update it while working; do not use chat history as the only progress record.
