# Handoff — OCR-GEOM-001

- **UTC:** 2026-09-05T17:57:45.305296+00:00
- **Agent:** claude
- **Commit/checkpoint:** cc7f807
- **Verification command:** `python scripts/verify_repo.py PASS (12 steps, none skipped), 2026-09-05; tests/unit/test_{lattice,ink,page_geometry}.py, tests/contracts/test_{extract_facts_lattice,drbx_ink_pass,cell_ink_pass}.py; refresh comparison 0 values swapped; both exports re-scored against the labels.`

## Completed
The printed table read as a grid, and the ink passes hardened by an adversarial review. ocr/lattice.py turns the rulings the geometry pass already stored into rows and cells; extract_facts.bind_in_lattice places a label the recognizer could not read from the form's template (virtual anchor, only on a page that fits it and only on a form that prints the locus on its values, so the value's own prefix corroborates the row) and retries an empty labelled cell in its ruled row: +2,272 cells, 0 values swapped. drbx_ink_pass.py certifies the other DRB3/4/5 genes ABSENT when the empty slot measures as paper (4,540 genes; 287 rows refused because their only token rests on the S-for-5 repair, KI-026). cell_ink_pass.py measures the ~24,000 empty labelled cells (14,070 paper, 3,457 ink no engine boxed) and WRITES NO FACT: NOT_TESTED is HA-009's per-laboratory verdict, so HA-012 puts the per-page question to the human. Ink counts only glyph-shaped components and certifies nothing on a page whose own print the measure cannot see (the control). Adversarial review: 5 lenses, 122 agents, 35 confirmed findings (16 high), all fixed or refused — the ruled-row retry gated to prefixed forms, a vertical must cross 60% of a row, a band containing another ruling is refused, tilted rulings read at the x asked about, the DRB3/4/5 slot read from the grid rather than assumed, virtual-anchor provenance mapped back to the stored frame, stale measurements withdrawn. Also this session: Tesseract over the 5,859 changed cells; the upright-crop ablation closed as a wash (M4). Labelled export 2 holds at 87/71/23/4/2; every ink-certified absence matches the human's ABSENT and none contradicts a value.

## Next
Human: HA-012 (may a measured empty cell be NOT_TESTED per page? ~15,000 review items) and HA-008 labelling — the promoted cells, the repaired DRB3/4/5 slots and the ink-certified absences are the three rules the labels can settle. Agent: KI-027 (a grid from merged, gap-bridged rulings: 5,581 empty cells have no ruled row today, and the DRB3/4/5 lattice fallback places only 26 of 1,089); M6 after HA-011; KI-023's OpenCV override on a machine that can run the gate twice. After any refresh: decode, the three confirm targets, promote_proposals.py, drbx_ink_pass.py, cell_ink_pass.py.

## Known failures/blockers
None declared by handoff script. Add specific failures here if any verification did not pass.

## Human actions
See `docs/agent-memory/HUMAN_ACTIONS.md`. Never put secret values here.
