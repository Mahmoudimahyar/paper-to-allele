# Handoff — OCR-GEOM-001

- **UTC:** 2026-09-05T23:46:27.436976+00:00
- **Agent:** claude
- **Commit/checkpoint:** d4a5d8f
- **Verification command:** `python scripts/verify_repo.py PASS, 12 steps, none skipped`

## Completed
Traced every non-correct cell of the reviewer's 220-label export and fixed what the corpus bore out. Two of the resolver's gates refuse a cell for what the recognizer produced rather than where the box sits; reread_refused.py gives those 5,145 cells a second opinion and takes a value only when PP-OCRv6 and PP-OCRv5-server both state it, it parses, it is admissible and it differs from what was refused (+2,926 cells; single-engine yield was 97%, two-engine 75%). A bare A/B/C now anchors when the page's structure makes it a label, never on its text (+106 per 4,000 documents, none changed). Ownership between two aligned labels needs a quarter-label-height margin so the tilt of a photograph cannot decide which gene owns a value. Full refresh plus every pass: RESOLVED 108,025 to 110,893 across the corpus, and the labels moved from 103 correct to 107 with no new contradiction. Two instrument defects found: the page had shown no crop for 50 of the 220 answers, and a pack rebuild reshuffles the stratified sample so 11 of 220 labels survived it; review_pack.py --keep-labelled pins them and all 220 now attach, 189 with a crop.

## Next
HA-014: the human re-answers the seven NOT_PRINTED cells that now carry crops. HA-013: the Google Vision key is absent from .env and CV_RESEARCH s6 still forbids sending an image off the machine. Then the 14 remaining misses: 6 have no anchor at all, 6 are found and refused on reading-axis ownership, max_values, two anchors on one row or a star-less value.

## Known failures/blockers
None declared by handoff script. Add specific failures here if any verification did not pass.

## Human actions
See `docs/agent-memory/HUMAN_ACTIONS.md`. Never put secret values here.
