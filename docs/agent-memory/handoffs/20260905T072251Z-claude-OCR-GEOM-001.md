# Handoff — OCR-GEOM-001

- **UTC:** 2026-09-05T07:22:51.237596+00:00
- **Agent:** claude
- **Commit/checkpoint:** 028e13d
- **Verification command:** `python scripts/verify_repo.py (see the commit); tests/unit/test_{ocr_glyphs,grouped_drbx,row_tolerance,prefix_filter,confirm_gene,rulings}.py, tests/contracts/test_{confirm_targets,promote_proposals,refresh_facts,review_pack}.py; scripts/pack_score.py on both exports; refresh comparison 0 swapped.`

## Completed
Traced every non-correct cell of the reviewer's 187-cell export to a root cause and fixed each where the corpus bore it out (ADR 0009 s7): label canonicaliser v2 (DQB1 read as DOBI; +2,411 DQB1 resolved), prefix-consistent filtering on the family rule (+459), a tolerance band as a fallback on the default rule (+901 cells, 890 second alleles), grouped-header v2 (+1,056 gene calls), the repaired-slot rule (KI-026), PP-OCRv5 targets over proposals and gene boxes with a gated promotion (760 promoted, never on LOW pages, re-judged with admissibility; 44% of proposals contradicted), geometry v3 (sweep bound 2 deg, agreement 3 deg/0.7; 6,511 ROTATE / 2,916 UNCERTAIN), the pack shows the Telegram messages with each image. Facts refreshed twice: HLA+DRB3/4/5 RESOLVED 82,480 -> 86,399, 0 values swapped; refresh_facts.py now prints the per-locus comparison (it caught the LAB-as-HLA-B bug before it shipped). Adversarial review: 133 agents, 24 findings, all fixed. Labelled export: 78/74/29/4/2 -> 83/71/27/4/2 (correct/abstained/missed/contradicted/partial).

## Next
Reviewer keeps labelling (HA-008): the repaired DRB3/4/5 slots and the promoted cells are the two rules the labels can settle; NOT_PRINTED accepted by Enter is a default. Agent: Tesseract over the changed cells; the --frame decode ablation (M4); M5 lattice (ink-certified empty second column); reinstate-on-confirmer for KI-026; the next pack with the tilted stratum only after this one is finished; KI-023.

## Known failures/blockers
None declared by handoff script. Add specific failures here if any verification did not pass.

## Human actions
See `docs/agent-memory/HUMAN_ACTIONS.md`. Never put secret values here.
