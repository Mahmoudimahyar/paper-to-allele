# Handoff — MATCH-001

- **UTC:** 2026-09-08T15:52:15.477651+00:00
- **Agent:** claude
- **Commit/checkpoint:** 04b0e3a
- **Verification command:** `scripts/verify_repo.py PASS (12 steps, none skipped); acceptance.py run MATCH-001 PASS; rank_matches.py --summary byte-identical across two runs`

## Completed
MATCH-001 v2.0.0 implemented and tested: bidirectional pre-screen ranking over UK Kidney Allocation Scheme levels re-cut on the HLA-DR gate. src/kidneymatch/matching/ (mismatch, gates, ranking, policy, core) plus scripts/rank_matches.py. 760 matching tests; 100% of statements and branches on every module. The test suite found six real bugs, all fixed: bare-token locus normalisation, an UNKNOWN locus charged nothing (so deleting a mismatched typing improved a rank), an unreadable blood group reaching the ranked PROVISIONAL_ABO bucket, the anchor role never checked, a donor-side NEGATIVE crossmatch masking a recipient-side POSITIVE, and the evidence penalty skipping UNKNOWN. All seven acceptance criteria met with evidence; all ten spec invariants carry covering tests.

## Next
MATCH-001 is ACTIVE with zero completion blockers but is deliberately not COMPLETE: the spec is blocked_by HA-004 and the policy is DESIGN_NOT_ADOPTED. Adoption is a human decision; see the HA-004 entry for what is built, what adoption changes, and why no archive pair can reach RANKED (every Gold blood group is patient-reported, KI-014). Siblings MATCH-ABO-001, MATCH-HLA-001, MATCH-IMMUNE-001, MATCH-EXPLAIN-001 and MATCH-EVAL-001 are still BACKLOG although their code and tests now exist.

## Known failures/blockers
None declared by handoff script. Add specific failures here if any verification did not pass.

## Human actions
See `docs/agent-memory/HUMAN_ACTIONS.md`. Never put secret values here.
